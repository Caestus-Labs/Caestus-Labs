// Caestus Labs — site config
//
// Copy this file to `js/config.js` and fill in your Supabase project values.
// The anon key is safe to ship to clients (it's gated by RLS policies).
//
// `js/config.js` is in .gitignore so real keys never enter git.

window.SUPABASE_URL = 'https://YOUR_PROJECT_REF.supabase.co';
window.SUPABASE_ANON_KEY = 'YOUR_ANON_PUBLIC_KEY';
